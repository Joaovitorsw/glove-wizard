import { FormatTypedDateDirective } from './format-typed-date.directive';

describe('FormatTypedDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatTypedDateDirective();
    expect(directive).toBeTruthy();
  });
});
